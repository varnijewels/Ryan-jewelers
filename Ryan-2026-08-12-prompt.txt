RYAN JEWELS — DEVELOPMENT HANDOFF PROMPT
Date: 2026-08-12
Timezone: Asia/Kolkata

Use this prompt to continue the Ryan Jewels website work from the current state.

PROJECT
-------
Source workspace:
/Users/bhaktidesign7/Documents/projects/Ryan Jewels

Reliable Git/test checkout:
/private/tmp/ryan-push-20260812

Repository:
https://github.com/varnijewels/Ryan-jewelers.git

Branch: main

IMPORTANT GIT / ICLOUD NOTE
---------------------------
- The source workspace is stored in iCloud. Some files can hang or return delayed output.
- The reliable temp checkout was used for builds, tests, browser checks, and mirroring changes.
- Today’s changes are local and have NOT been committed, pushed, or deployed.
- Do not reset, discard, overwrite, commit, push, or deploy without explicit user approval.
- Preserve unrelated user changes in the working tree.
- Do not open Cursor unless the user explicitly requests it.

LIVE AND LOCAL URLS
-------------------
- Live website: https://ryan.varnijewels.com
- Primary local website: http://localhost:3000
- Clean verification server used today: http://127.0.0.1:3010
- Before starting localhost, check which process is using ports 3000/3001/3010.

USER REQUIREMENTS CONFIRMED TODAY
---------------------------------
- “Browse By Collection” must always be the final group in every mega menu.
- Its background must match the mega-menu background.
- Collection product image and title must come from admin-store product listings.
- Most-clicked products should rank first after the website is live.
- New or empty categories must display “Coming Soon.”
- Search must remain focused and fully functional.
- Search suggestions must show similar products with image, category, title, price, and count.
- The search query must remain visible after navigating to the product listing.
- The listing banner must show the search query instead of an unrelated category title.
- Best Offers must not show the existing discount listing until a final design is approved.
- Best Offers should temporarily show a polished “Coming Soon” page.

COMPLETED WORK
--------------
1. Fixed “Browse By Collection” placement so it remains last in every admin-driven mega menu.
2. Added fallback “Browse By Collection” groups when admin menu data does not include one.
3. Matched the collection column background with the mega-menu background.
4. Added dynamic product cards sourced from admin product listings.
5. Added product image and title rendering from actual product data.
6. Added “Coming Soon” cards for empty/new categories.
7. Preserved the requested fixed decorative ring/earring artwork in mega menus.
8. Created most-clicked product tracking APIs:
   GET /api/popularity?category=<slug>&limit=2
   POST /api/popularity/update
9. Added per-visitor daily click deduplication.
10. Added Redis/Upstash support for production and a temporary-file fallback for local development.
11. Added product-page click tracking.
12. Connected popularity ranking to “Browse By Collection.”
13. Fixed the Ryan header search root cause: the shared modal renderer blurred the inline input immediately after focus.
14. Replaced only the Ryan inline search behavior with the existing product search service.
15. Added a 250 ms debounced autocomplete request.
16. Added stale-request protection so old search responses cannot replace newer results.
17. Added a “Similar Results” panel with up to six products.
18. Added suggestion images, categories, titles, prices, arrows, total result count, loading skeletons, and empty state.
19. Added keyboard behavior: Enter opens the full listing and Escape closes suggestions.
20. Added outside-click closing and a working search icon submit button.
21. Added a “View all results for query” action.
22. Retained the query in the header search field after navigation.
23. Changed the listing banner to “Search Results for query.”
24. Updated the listing breadcrumb to use the searched query.
25. Changed Best Offers navigation from the discount listing to /best-offers.
26. Created a responsive “Best Offers — Coming Soon” page.
27. Reviewed today’s changes for unnecessary/over-engineered code.
28. Confirmed the main feature code is reasonably lean.
29. Confirmed the production SvelteKit build completes successfully.

PRIMARY CHANGED FILES
---------------------
src/lib/theme/ryans-jewels/RjAdminMegaMenu.svelte
src/lib/theme/ryans-jewels/RyansJewelsNav.svelte
src/lib/theme/ryans-jewels/RyansJewelsListingPage.svelte
src/lib/theme/ryans-jewels/admin-menu.ts
src/lib/theme/ryans-jewels/nav-content.ts
src/lib/server/product-popularity.ts
src/routes/api/popularity/+server.ts
src/routes/api/popularity/update/+server.ts
src/routes/(www)/products/[slug]/+page.svelte
src/routes/(www)/best-offers/+page.svelte
tests/navigation.spec.ts
tests/ryans-admin-menu.test.ts
tests/product-popularity.test.ts
vite.config.ts

VERIFICATION COMPLETED
----------------------
- Search browser verification passed locally:
  input remained focused
  typed value remained “ring”
  /api/ms/products returned HTTP 200
  result panel opened
  total count displayed 10 products
  six similar-product cards displayed
  browser page errors: zero
- Focused Vitest result:
  2 test files passed
  4 tests passed
- Five changed Svelte components compiled successfully.
- Production build completed successfully.
- git diff --check passed for the focused feature files.
- Existing repository-wide Svelte warnings remain; they were already present and are unrelated to today’s feature work.

CURRENT BLOCKERS: 2
-------------------
1. LIVE STOREFRONT / BACKEND CONNECTIVITY
   - ryan.varnijewels.com did not establish an HTTPS connection during direct checks.
   - api.varnijewels.com also timed out.
   - Local server logs showed EHOSTUNREACH / ETIMEDOUT for:
     api/stores/public-details?store_id=store_01KHE7ZJ6951Y73FFRWKJDSNRN
   - DNS resolves both domains to Cloudflare IP addresses.
   - This points to hosting/origin/backend connectivity, not a confirmed frontend compile failure.
   - Because store data is required during SSR, the unavailable backend can make storefront routes return HTTP 500.

2. PRODUCTION POPULARITY STORAGE CONFIGURATION
   - No Upstash/KV Redis URL or token is currently configured in the checked local environment.
   - Production popularity update requests intentionally return HTTP 503 when Redis is not configured.
   - Configure one supported pair in the production hosting environment:
     UPSTASH_REDIS_REST_URL + UPSTASH_REDIS_REST_TOKEN
     or KV_REST_API_URL + KV_REST_API_TOKEN
   - Never place production tokens in this prompt or commit them to Git.

MINOR NON-BLOCKING CODE ISSUES: 2
---------------------------------
1. RjAdminMegaMenu can merge a previously ranked product that is no longer present in the current admin listing. Filter ranked products against the current listing map before rendering so removed/unpublished products never appear.
2. RyansJewelsListingPage uses `data.products?.count || 265`; a real zero count displays 265. Replace it with nullish fallback logic, for example `data.products?.count ?? 0`.

CODE CLEANUP NOTE
-----------------
- `.data/product-popularity.json` is generated runtime data and should not be committed.
- Local popularity code now defaults to the operating-system temporary directory.
- Add `.data/` to .gitignore or remove the generated file before a future commit.
- The `emptyState()` one-caller helper could be inlined, but this is optional and not a functional issue.

NEXT ACTIONS
------------
1. Fix the two minor code issues above and rerun the focused tests/build.
2. Configure production Redis variables through the hosting provider’s secret/environment settings.
3. Check or restart the live origin/backend service serving api.varnijewels.com.
4. Check Cloudflare origin reachability, proxy, SSL/TLS, firewall, and DNS origin configuration.
5. Verify /api/stores/public-details returns successfully before testing storefront pages.
6. After backend recovery, test live home, women, product search, product details, mega menus, and /best-offers.
7. Verify the most-clicked API on production after Redis configuration.
8. Verify removed/unpublished products never appear in Browse By Collection.
9. Commit, push, and deploy only after explicit user approval.

DO NOT CLAIM YET
----------------
- Do not say the live website is fully working until backend/origin connectivity is restored and live routes are retested.
- Do not say production popularity tracking is working until Redis is configured and both API endpoints are verified live.
- Do not say Best Offers is live until today’s local changes are committed, deployed, and verified.
- Do not commit `.data/product-popularity.json`.

END OF HANDOFF PROMPT
